<Page x:Class="MEnglish.Views.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MEnglish.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:Microsoft.UI.Xaml.Controls"
      xmlns:media="using:Microsoft.UI.Xaml.Media"
      xmlns:models="using:MEnglish.Models"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      controls:BackdropMaterial.ApplyToRootOrPageBackground="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="Рейтинг слов"
                       FontSize="64"
                       FontWeight="Bold"
                       FontFamily="Bahnschrift"
                       FontStretch="Condensed"
                       HorizontalAlignment="Center" />
            <GridView ItemsSource="{x:Bind Words.All}"
                      SelectionMode="None"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="models:Word">
                        <Grid>
                            <StackPanel CornerRadius="32">
                                <StackPanel.Background>
                                    <media:AcrylicBrush TintColor="Gray"
                                                        Opacity=".1" />
                                </StackPanel.Background>
                                <Border CornerRadius="32 32 0 0">
                                    <Image Height="160"
                                           Width="160"
                                           HorizontalAlignment="Left"
                                           Source="{x:Bind Image}"
                                           Stretch="UniformToFill" />
                                </Border>
                                <ProgressBar Value="{x:Bind Rating}"
                                             Height="10"
                                             HorizontalAlignment="Stretch">
                                    <ProgressBar.Foreground>
                                        <LinearGradientBrush>
                                            <GradientStop Color="LightSeaGreen" />
                                            <GradientStop Color="LightGreen"
                                                          Offset=".5" />
                                            <GradientStop Color="GreenYellow"
                                                          Offset="1" />
                                        </LinearGradientBrush>
                                    </ProgressBar.Foreground>
                                </ProgressBar>
                                <TextBlock Text="{x:Bind EnglishForms[0]}"
                                           FontSize="32"
                                           FontWeight="Bold"
                                           FontFamily="Bahnschrift"
                                           Margin="10 8 0 0" />
                                <TextBlock Text="{x:Bind RussianForms[0]}"
                                           FontSize="24"
                                           FontWeight="Bold"
                                           FontFamily="Bahnschrift"
                                           Margin="10 0" />
                                <TextBlock Text="{x:Bind IsLearned}"
                                           Margin="10 0 0 16" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </StackPanel>
    </Grid>
</Page>
